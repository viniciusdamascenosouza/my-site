import * as S from "./styled";
import ArrowUpwardIcon from "@mui/icons-material/ArrowUpward";

const ScrollButton = () => {
  return (
    <S.ScrollButton smooth>
        <ArrowUpwardIcon />
    </S.ScrollButton>
  );
};

export default ScrollButton;
