import styled from "styled-components";

export const ImgDefault = styled.img`
  width: 280px;
  height: 380px;
  object-fit: contain;

  filter: blur(0.5px);
  transition: filter 0.3s;
  opacity: 0.7;
`;
